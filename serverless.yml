service: serverless-aws-project

frameworkVersion: '4'

provider:
  name: aws
  runtime: nodejs18.x
  region: us-east-1

resources:
  Resources:
    MyS3Bucket:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: s3-serverless-aws-oidc1

org: govin87
app: aws-app

